apiVersion: v1
kind: Secret
metadata:
  name: lldap-secret
  labels:
    app: lldap
type: Opaque
stringData:
  # IMPORTANT: Override these before deploying.
  #   oc create secret generic lldap-secret \
  #     --from-literal=jwt-secret='<random-string>' \
  #     --from-literal=admin-password='<strong-password>'
  jwt-secret: "changeme-use-a-long-random-string"
  admin-password: "changeme"
