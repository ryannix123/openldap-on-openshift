apiVersion: v1
kind: Secret
metadata:
  name: openldap-admin
  labels:
    app: openldap
type: Opaque
stringData:
  # IMPORTANT: Override these before deploying.
  # Recommended: use Sealed Secrets, Vault, or `oc create secret generic`
  # instead of committing real passwords to source control.
  #
  #   oc create secret generic openldap-admin \
  #     --from-literal=admin-password='<strong-password>' \
  #     --from-literal=readonly-password='<strong-password>'
  admin-password: "changeme"
  readonly-password: "readonly"
